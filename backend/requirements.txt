## 🧠 Implementation

The credibility scoring engine is implemented in Python and evaluates traders using risk-adjusted metrics.

See: `/backend/scoring.py`
Add FastAPI + Uvicorn:
fastapi
uvicorn
numpy
▶️ Step 4: Run the API

In terminal:
cd backend
pip install -r requirements.txt
uvicorn main:app --reload
